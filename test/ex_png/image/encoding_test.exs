defmodule ExPng.Image.EncodingTest do
  use ExUnit.Case

  describe "calculate_bit_depth" do
    test "for a pure black and white image it returns 1" do

    end

    test "for a grayscale image it returns 8" do

    end

    test "for a grayscale image with transparency it returns 8" do

    end

    test "for a color image it returns 8" do

    end

    test "for a color image with transparency it returns 8" do

    end
  end

  describe "calculate_color_type" do
    test "for a black and white image with no transparency it returns @grayscale" do
    end
  end
end
